# Presentation update — only new material from 2026-09-15

## New slide A — Camera-resolution forensics

**On slide**
- Chronos 1.4 manufacturer full-frame capability: **1280 × 1024 @ 1069 FPS**.
- Project analysis code explicitly resamples PIV fields to **1630 × 1345** using bilinear interpolation.
- Therefore 1630 × 1345 cannot be treated as native acquisition resolution from that analysis path.
- Canonical encoded resolution remains an M0 `ffprobe` output.

**Say:** “We found the likely source of one resolution value: 1630 by 1345 is explicitly generated by post-processing code. I am therefore separating sensor capability, encoded acquisition size and later interpolation rather than calling all of them camera resolution.”

**Do not claim:** that the canonical MP4 is already proven to be 1280 × 1024.

## New slide B — Quantified vector-grid contradiction

**On slide**
- Raw exported pitch: **0.0034310519 m**.
- Calibration: **0.00019061 m/px**.
- Exported step: **18.000377 px ≈ 18 px**.
- Claimed final 18 px window + 50% overlap would imply **9 px**.
- Historical first pass: **INSUFFICIENT_EVIDENCE** (32/36/64 documentary conflict).

**Say:** “The conflict is no longer qualitative. The exported coordinates themselves encode an approximately 18-pixel node spacing, so the documented 18-pixel final window with 50 percent overlap cannot be interpreted naively as the grid that produced these exported nodes.”

**Do not claim:** that 0% overlap, 32 px or 36 px has been proven.

## New slide C — M0 execution is now operationally specified

**On slide**
- Connector transfer ceiling independently confirmed even in streamed-reference mode.
- New M0-only runner binds exact Drive ID → provider MD5 → mounted bytes → complete SHA-256.
- Strict FFmpeg EOF decode + explicit 4250–4500 range check.
- JSON + CSV + Markdown + script/Git hash manifest.
- **M1 remains locked until PASS.**

**Say:** “The next action is no longer ‘find a way to download the video’. The exact Colab execution route and fail-closed identity chain are implemented. One canonical run will produce the missing publication-grade M0 package.”
